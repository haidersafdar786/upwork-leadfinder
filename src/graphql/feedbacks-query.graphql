query FreeLancerFeedBacks($freelancerId: ID!, $filter: ReviewsFeedBackFilter) {
  freelancerFeedBacks(freelancerId: $freelancerId, filter: $filter) {
    totalItems
    feedbacks {
      score
      text
      assignmentEndedOn
      assignmentTitle
      clientFirstName
      clientLastName
    }
  }
}
